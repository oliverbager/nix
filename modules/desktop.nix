{ config, pkgs, ... }: 

{  
  # desktop only module directories
  imports = [
    # modules
  ];

  # desktop only packages
  home.packages = with pkgs; [
    
    # packages
  ];

  #other options
}
